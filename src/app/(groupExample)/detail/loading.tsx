//Puedes envolver el componente a mostrar la pantalla de carga con <Suspense fallback={contenido a mostrar hasta que cargue el componente}/>

const loading = () => <h3>Loading...</h3>

export default loading ;